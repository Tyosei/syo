# Lark 资源进度机器人 + 可视化筛选看板（落地方案）

## 1. 目标与现状

你当前通过表格管理外包资源进度（版本 / 角色 / ID / 资源进度 / 风险），痛点是：

1. 在 Lark 群里讨论时还要频繁切出打开表格查询。
2. 通知策划时信息分散，风险同步不及时。
3. 缺少一个可快速筛选与汇总的可视化界面。

本方案目标：

- 在 **Lark 群里@机器人即可查询进度与风险**。
- 提供一个 **Web 可视化看板**，支持按版本/角色/ID/状态筛选。
- 形成“外包更新 → PM校验 → 自动通知策划”的闭环流程。

---

## 2. 整体方案（推荐）

```text
外包/PM维护表格（Lark Sheets 或 Google Sheets）
        ↓（定时拉取 / Webhook）
   后端服务（Python FastAPI）
      ├─ 进度查询 API
      ├─ 风险识别 API
      ├─ Lark Bot 指令处理
      └─ 消息推送（群通知）
        ↓
   1) Lark 群聊机器人
   2) 可视化看板（Vue/React）
```

技术栈建议：

- 后端：FastAPI + SQLModel/PostgreSQL（初期可 SQLite）
- 定时任务：APScheduler / Celery
- 前端：Vue3 + Element Plus（筛选表格效率高）
- 机器人：Lark Bot（事件订阅 + 消息卡片）
- 部署：Docker + Nginx

---

## 3. 表结构建议（先统一口径）

建议你把当前表格字段统一成以下核心列：

- `version`：版本号（如 1.4.2）
- `asset_type`：资源类型（角色立绘 / 3D模型 / 动效 / 音频）
- `role_name`：角色名
- `resource_id`：资源ID（唯一）
- `vendor`：外包方
- `owner`：内部跟进人
- `plan_date`：计划完成日期
- `actual_date`：实际完成日期
- `progress`：进度（0-100）
- `status`：状态（未开始/进行中/待验收/已完成/阻塞）
- `risk_level`：风险等级（低/中/高）
- `risk_desc`：风险说明
- `next_action`：下一步动作
- `updated_at`：更新时间

关键规则：

1. `resource_id` 全局唯一，避免同名资源混淆。
2. `status` 使用固定枚举，便于机器人统计。
3. 风险定义标准化（例如：预计延期>3天=中风险，>7天=高风险）。

---

## 4. Lark 机器人指令设计（群里可直接查）

建议先做 6 条高频指令：

1. `@bot 进度总览`  
   返回：各状态数量、本周完成数、延期数、高风险数。

2. `@bot 查询 版本=1.4.2`  
   返回：该版本资源列表（可限制前20条）+ 统计。

3. `@bot 查询 角色=琳`  
   返回：角色相关资源的当前状态、阻塞原因。

4. `@bot 查询 id=CHR_1024`  
   返回：单资源全量信息（计划、实际、风险、责任人）。

5. `@bot 风险列表`  
   返回：中高风险资源，按计划日期升序。

6. `@bot 催办 vendor=外包A`  
   返回：外包A名下逾期或临期资源（可@对应负责人）。

机器人回复形态建议：

- 优先使用 **Lark 交互式消息卡片**（可读性高、可点击筛选跳转看板）。
- 大结果集默认只展示前 N 条，并附“查看完整列表”链接到看板。

---

## 5. 可视化看板需求（你要的筛选界面）

页面建议分三块：

### A. 顶部 KPI

- 总资源数
- 已完成数 / 完成率
- 进行中数
- 高风险数
- 本周应交付数 / 已交付数

### B. 多条件筛选区

支持组合筛选：

- 版本（多选）
- 角色（模糊搜索）
- 资源ID（精确搜索）
- 资源类型
- 外包方
- 状态
- 风险等级
- 日期区间（计划日期）

### C. 数据表 + 导出

表格列建议：

`版本 | 角色 | 资源ID | 类型 | 外包 | 进度 | 状态 | 风险等级 | 计划日期 | 实际日期 | 负责人 | 更新时间`

能力建议：

- 颜色标记：高风险红色、阻塞橙色。
- 行内快捷操作：更新状态、填写风险、@通知。
- 导出：按当前筛选结果导出 Excel。

---

## 6. 通知与风险闭环（重点）

你提到要“把资源进度和风险通知实装资源到游戏的策划”，建议流程如下：

1. 每天 10:00 自动生成《版本进度日报》发到策划群。
2. 每天 18:00 自动生成《风险清单》发到项目群。
3. 当资源状态变为“阻塞/高风险”时，实时推送告警。
4. 告警消息必须包含：资源ID、影响版本、影响内容、责任人、预计恢复时间。

消息模板示例：

- 标题：`[高风险] 1.4.2 版本资源延期预警`
- 内容：`CHR_1024（角色：琳）预计延期 5 天，影响主线章节3过场动画实装。责任人：张三。`
- 按钮：`查看看板` / `确认已知` / `更新预计完成时间`

---

## 7. 分阶段落地（4周）

### 第1周：数据治理

- 清洗现有表格字段与枚举。
- 明确风险分级规则。
- 建立统一数据表结构。

### 第2周：机器人 MVP

- 完成 `进度总览 / 查询 / 风险列表` 三个核心指令。
- 在测试群灰度试运行。

### 第3周：可视化看板

- 完成筛选、列表、导出。
- 打通“Lark 卡片跳转看板”。

### 第4周：自动通知与权限

- 日报/风险播报自动化。
- 增加权限控制（仅PM可编辑、策划只读）。
- 发布到正式群并收集优化项。

---

## 8. MVP 验收标准（建议）

满足以下条件即可上线：

1. 群里查询响应时间 ≤ 3 秒（95分位）。
2. 机器人查询结果与表格原始数据一致率 ≥ 99%。
3. 支持版本/角色/ID 三种核心筛选。
4. 每日自动风险播报成功率 ≥ 99%。
5. 试运行一周内，PM手工打开表格次数降低 50% 以上。

---

## 9. 你可以立刻执行的下一步

1. 先冻结并确认当前表格字段（按第3节）。
2. 拉一个 30 分钟会，统一“风险等级判定口径”。
3. 指定一个版本（如 1.4.2）做 MVP 试点。
4. 我们可以下一步直接输出：
   - Lark Bot 指令文案与卡片 JSON 模板
   - FastAPI 接口定义（OpenAPI）
   - 看板页面线框图

## 10. 与 Lark 多维表格能力重叠分析（你关心的问题）

结论先说：**有一部分功能确实重叠**，建议采用“**Lark 原生优先 + 自研补差**”策略。

### 10.1 明显重叠（优先直接用 Lark）

1. 基础字段管理（版本/角色/ID/状态/负责人）
2. 视图筛选（按版本、负责人、状态筛选）
3. 看板/甘特等基础可视化
4. 基础自动提醒（到期提醒、简单机器人通知）

这些能力用多维表格就能较快上线，不建议一开始重复造轮子。

### 10.2 不完全重叠（建议自研）

1. **群聊自然语言查询体验**：
   - 例如 `@bot 查询 版本=1.4.2 且 高风险 且 外包A` 的组合语义查询。
2. **跨表/跨项目聚合统计**：
   - 多个项目、多张表统一口径统计（统一风险算法）。
3. **复杂风险规则引擎**：
   - 例如“连续两次延期 + 关键路径资源”自动升级为高风险。
4. **与游戏研发链路的联动**：
   - 与版本实装节点、资产流水线、CI状态做联动告警。
5. **管理层仪表盘**：
   - 需要定制化指标、权限、留痕与审计时，自研价值更高。

### 10.3 推荐实施边界（避免重复建设）

- **阶段A（2~4周）**：
  - 用 Lark 多维表格承载主数据与筛选视图。
  - 机器人先做“读多维表格并回群结果”的轻量层。
- **阶段B（4~8周）**：
  - 仅补充 Lark 不擅长的能力：复杂规则引擎、跨项目聚合、管理驾驶舱。
- **阶段C（按需）**：
  - 当数据量、权限模型、系统联动复杂度上升，再逐步沉淀独立后台。

### 10.4 一句话决策标准

如果某个需求在 Lark 多维表格内 **1天内可配置上线**，就不要自研；
只有当需求涉及 **跨系统联动、复杂规则、规模化治理** 时，才进入自研范围。

## 11. 自研量最低方案（1周可落地）

目标：在尽量不开发独立系统的前提下，完成“群里可查 + 可筛选 + 可通知”。

### 11.1 方案原则

1. **数据层不自研**：直接使用 Lark 多维表格作为唯一数据源。
2. **前端不自研**：先不做独立 Web 看板，直接用多维表格视图充当看板。
3. **后端最小化**：只做一个轻量 Bot 服务（查询转发 + 格式化回复）。
4. **通知配置化**：优先用 Lark 自动化，减少代码定时任务。

### 11.2 最小架构

```text
Lark 多维表格（主数据 + 视图）
   ├─ 视图A：按版本
   ├─ 视图B：高风险
   ├─ 视图C：本周交付
   └─ 自动化：到期/逾期提醒
            ↓
      轻量 Lark Bot（仅查询）
            ↓
        项目群 / 策划群
```

实现边界：

- Bot 只提供 3 个命令：
  - `@bot 进度总览`
  - `@bot 查询 版本=xxx`
  - `@bot 风险列表`
- Bot 不做复杂规则引擎，不做跨系统集成，不建独立数据库。

### 11.3 1周执行清单

**Day 1（0.5~1天）**：整理表结构
- 按第3节字段统一列名与枚举。
- 增加 3 个必备字段：`status` / `risk_level` / `updated_at`。

**Day 2（0.5天）**：配置多维表格视图
- 建 3 个视图：版本视图、高风险视图、本周交付视图。
- 颜色规则：高风险红色、阻塞橙色。

**Day 3（1天）**：配置自动化
- 逾期提醒自动推送到项目群。
- 高风险状态变更时自动通知策划群。

**Day 4-5（1~2天）**：上线轻量 Bot
- Bot 从多维表格读取数据并返回统计卡片。
- 支持上面 3 条命令。
- 只返回前20条并附视图链接。

**Day 6（0.5天）**：灰度与修正
- 在测试群跑 1 天，修正文案和筛选条件。

**Day 7（0.5天）**：正式上线
- 发布使用手册（命令清单 + 视图链接）。

### 11.4 人力与投入（最低配）

- PM：1人（字段治理 + 验收）
- 开发：0.5~1人（Bot 轻量服务）
- 总工期：约 5~7 个工作日

### 11.5 最低配验收标准

1. 群聊内 3 秒内返回查询结果。
2. 80% 日常查询可通过 3 条命令覆盖。
3. 策划不再需要手动催要进度截图。
4. 高风险项可在当日自动触达相关群。

### 11.6 何时再升级到“中度自研”

满足任一条件再升级：

- 同时维护 3 个以上项目，跨表统计变重；
- 需要复杂风险评分模型；
- 需要对接版本发布系统/资产流水线；
- 对权限审计和历史追溯有强合规要求。

